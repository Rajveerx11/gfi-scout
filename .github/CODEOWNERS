# @Rajveerx11 owns every path in this repo (default reviewer, notified on PRs).
# Note: code-owner review is NOT currently required to merge — branch
# protection on `main` sets required approving reviews to 0 (solo repo).
# Re-enable "Require review from Code Owners" if a second maintainer joins.

* @Rajveerx11
